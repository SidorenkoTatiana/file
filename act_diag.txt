@startuml
start
:Менеджер начинает проверку посылок;
repeat
  :Проверить очередную посылку;
  if (Данные корректны?) then (да)
    :Подтвердить сведения;
    :Присвоить статус "готово к доставке";
    :Сгенерировать внутренний номер;
  else (нет)
    if (Посылка отсутствует?) then (да)
      :Установить статус "ошибка, посылка отсутствует";
      :Добавить в накладную возврата;
    else (нет)
      :Вручную отредактировать данные;
      :Подтвердить исправленные сведения;
    endif
  endif
repeat while (Есть еще посылки?) is (да)
->нет;
:Завершить проверку;
stop
@enduml
